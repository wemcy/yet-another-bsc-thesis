openapi: 3.0.3
info:
  title: Recipe Management Web Application API
  description: Recipe create/list/update/delete operations with allergen-based filtering.
  contact:
    name: Máté Zsolt Sándor
    email: mzsoltsandor@gmail.com
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
  version: v0.1.0-dev
servers:
- url: http://localhost
  description: Dummy development server
- url: http://localhost:9393/api
  description: Local development
- url: http://raspberrypi/api
  description: Local network development
tags:
- name: Health
  description: Service health / availability
- name: Recipes
  description: Recipe operations
- name: Rating
  description: Ratings
- name: Comment
  description: Comments on recipes
- name: Auth
  description: User authentication
- name: Profile
  description: User profile operations
paths:
  "/search":
    get:
      tags:
      - Recipes
      summary: Search recipes by title
      operationId: searchRecipes
      parameters:
        - name: title
          in: query
          description: The title or part of the title to search for
          required: true
          schema:
            type: string
        - name: includeAllergens
          in: query
          description: List of allergens to include (comma-separated, e.g. "GLUTEN,PEANUTS")
          required: false
          schema:
            "$ref": "#/components/schemas/AllergenFilter"
        - name: excludeAllergens
          in: query
          description: List of allergens to exclude (comma-separated, e.g. "GLUTEN,PEANUTS")
          required: false
          schema:
            "$ref": "#/components/schemas/AllergenFilter"
      responses:
        '200':
          description: Recipes matching the search criteria
          content:
            application/json:
              schema:
                type: array
                items:
                  "$ref": "#/components/schemas/RecipeSummary"
  "/auth/register":
    post:
      tags:
      - Auth
      summary: User registration
      operationId: register
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/RegisterRequest"
      responses:
        '200':
          description: Registration successful
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/LoginResponse"
        '400':
          description: Invalid request (e.g., email already in use, weak password)
          content:
            application/json:
              schema:
                type: array
                items:
                  "$ref": "#/components/schemas/IdentityError"
  "/auth/login":
    post:
      tags:
      - Auth
      summary: Login
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/LoginRequest"
      responses:
        '200':
          description: Login successful; session cookie set
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/LoginResponse"

        '401':
          description: Invalid email address or password
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Invalid email or password
  "/auth/logout":
    post:
      tags:
      - Auth
      summary: Logout
      operationId: logout
      responses:
        '204':
          description: Logout successful; cookie removed
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security:
        - cookieAuth: []
  "/profile/me":
    get:
      tags:
      - Profile
      summary: Get the logged-in user profile
      operationId: getOwnProfile
      responses:
        '200':
          description: Request successful
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Profile"
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"

      security:
        - cookieAuth: []
    put:
      tags:
      - Profile
      summary: Update the logged-in user profile
      operationId: updateOwnProfile
      requestBody:
        required: false
        content:
          multipart/form-data:
            schema:
              "$ref": "#/components/schemas/UpdateProfileRequest"
      responses:
        '204':
          description: Update successful
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security:
        - cookieAuth: []
  "/profile/me/image":
    get:
      tags:
      - Profile
      summary: Get the logged-in user profile image
      operationId: getOwnProfileImage
      parameters: 
        - name: imageSize
          in: query
          description: Desired profile image size (e.g., "thumbnail", "medium", "large")
          required: false
          schema:
            "$ref": "#/components/schemas/ImageSize"
      responses:
        '200':
          description: Profile image retrieved successfully
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"

      security:
        - cookieAuth: []
  "/profile/{id}/image":
    get:
      tags:
      - Profile
      summary: Get user profile image by ID
      parameters:
        - name: id
          required: true
          in: path
          description: Unique identifier of the user to query (UUID)
          schema:
            type: string
            format: uuid
        - name: size
          in: query
          description: Desired image size (e.g., "thumbnail", "medium", "large")
          required: false
          schema:
            "$ref": "#/components/schemas/ImageSize"
      operationId: getProfileImageById
      responses:
        '200':
          description: Profile image retrieved successfully
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
        '404':
          description: Profile image or user not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"

  "/profile/{id}":
    get:
      tags:
      - Profile
      summary: Get user profile by ID
      parameters:
        - name: id
          required: true
          in: path
          description: Unique identifier of the user to query (UUID)
          schema:
            type: string
            format: uuid
      operationId: getProfileById
      responses:
        '200':
          description: User profile retrieved successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Profile"
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"

        '404':
          description: User not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"

      security:
        - cookieAuth: []
    put:
      tags:
      - Profile
      summary: Update user profile by ID
      parameters:
        - name: id
          required: true
          in: path
          description: Unique identifier of the user to update (UUID)
          schema:
            type: string
            format: uuid
      operationId: updateProfileById
      requestBody:
        required: false
        content:
          multipart/form-data:
            schema:
              "$ref": "#/components/schemas/UpdateProfileRequest"
      responses:
        '204':
          description: User profile updated successfully
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"

        '403':
          description: Not the owner of this user profile
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"

        '404':
          description: User not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security: 
        - cookieAuth: []
    delete:
      tags:
      - Profile
      summary: Delete user by ID
      parameters:
        - name: id
          required: true
          in: path
          description: Unique identifier of the user to delete (UUID)
          schema:
            type: string
            format: uuid
      operationId: deleteProfileById
      responses:
        '204':
          description: User deleted successfully
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '403':
          description: Not the owner of this user profile
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: User not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security:
        - cookieAuth: []
  "/profile/{id}/roles":
    post:
      tags:
      - Profile
      summary: Add role to user by ID
      parameters:
        - name: id
          required: true
          in: path
          description: Unique identifier of the user to update (UUID)
          schema:
            type: string
            format: uuid
      operationId: addUserRoleById
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - role
              properties:
                role:
                  "$ref": "#/components/schemas/UserRole"
      responses:
        '204':
          description: User role added successfully
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '403':
          description: Not authorized to perform this operation (only admins can modify roles)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: User not found
          content:
              application/json:
                schema:
                  "$ref": "#/components/schemas/ErrorResponse"
        '400':
          description: Invalid role provided
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"

      security:
        - cookieAuth: []
  "/profile/{id}/recipes":
    get:
      tags:
      - Recipes
      summary: List recipes by user ID
      parameters:
        - name: id
          required: true
          in: path
          description: Unique identifier of the user to query (UUID)
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          description: The page number to retrieve (starting from 0)
          required: false
          schema:
            type: integer
            minimum: 0
        - name: pageSize
          in: query
          description: The number of items per page (default is 25)
          required: false
          schema:
            type: integer
            minimum: 25
            maximum: 100
      operationId: getRecipesByAuthorId
      responses:
        '200':
          description: Recipes retrieved successfully
          content:
            application/json:
              schema:
                type: array
                items:
                  "$ref": "#/components/schemas/Recipe" 
          headers:
            X-Pagination:
              description: Pagination metadata in JSON format
              schema:
                "$ref": "#/components/schemas/PaginationMetadata"
  "/alive/":
    get:
      tags:
      - Health
      summary: Service health report
      operationId: getServiceReport
      responses:
        '200':
          description: Report is sent back
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/HealthReport"
  "/recipes/":
    get:
      tags:
      - Recipes
      summary: List recipes
      operationId: listRecipes
      parameters:
        - name: page
          in: query
          description: The page number to retrieve (starting from 0)
          required: false
          schema:
            type: integer
            minimum: 0
        - name: pageSize
          in: query
          description: The number of items per page (default is 25)
          required: false
          schema:
            type: integer
            minimum: 25
            maximum: 100
        - name: includeAllergens
          in: query
          description: List of allergens to include (comma-separated, e.g. "GLUTEN,PEANUTS")
          required: false
          style: form
          explode: false
          schema:
            "$ref": "#/components/schemas/AllergenFilter"
        - name: excludeAllergens
          in: query
          description: List of allergens to exclude (comma-separated, e.g. "GLUTEN,PEANUTS")
          required: false
          style: form
          explode: false
          schema:
            "$ref": "#/components/schemas/AllergenFilter"
      responses:
        '200':
          description: Recipes
          content:
            application/json:
              schema:
                type: array
                items:
                  "$ref": "#/components/schemas/Recipe"
          headers:
            X-Pagination:
              description: Pagination metadata in JSON format
              schema:
                "$ref": "#/components/schemas/PaginationMetadata"
    post:
      tags:
      - Recipes
      summary: Create a new recipe
      operationId: createRecipe
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/CreateRecipeRequest"
      responses:
        '201':
          description: Recipe created successfully
          headers:
            Location:
              description: URL of the newly created resource
              schema:
                type: string
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Recipe"
      security:
        - cookieAuth: []
  "/recipes/{id}/":
    get:
      tags:
      - Recipes
      summary: Get recipe by ID
      parameters: 
        - name: id
          required: true
          in: path
          description: Unique identifier of the recipe to query (UUID)
          schema:
            type: string
            format: uuid
      operationId: getRecipeById
      responses:
        '200':
          description: Recipe retrieved successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Recipe"
        '404':
          description: Recipe not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      tags:
      - Recipes
      summary: Delete recipe by ID
      parameters: 
        - name: id
          required: true
          in: path
          description: Unique identifier of the recipe to delete (UUID)
          schema:
            type: string
            format: uuid
      operationId: deleteRecipeById
      responses:
        '204':
          description: Recipe deleted successfully
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '403':
          description: Not authorized to delete this recipe
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Recipe not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security:
        - cookieAuth: []
    put:
      tags:
      - Recipes
      summary: Update recipe by ID
      parameters: 
        - name: id
          required: true
          in: path
          description: Unique identifier of the recipe to update (UUID)
          schema:
            type: string
            format: uuid
      operationId: updateRecipeById
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/CreateRecipeRequest"
      responses:
        '200':
          description: Recipe updated successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Recipe"
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '403':
          description: Not authorized to update this recipe
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Recipe not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security:
        - cookieAuth: []
  "/recipes/{id}/rate":
    put:
      tags:
      - Recipes
      summary: Rate recipe by ID (1-5 stars)
      parameters: 
        - name: id
          required: true
          in: path
          description: Unique identifier of the recipe to query (UUID)
          schema:
            type: string
            format: uuid
      operationId: rateRecipe
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - rating
              properties:
                rating:
                  type: integer
                  minimum: 1
                  maximum: 5
                  description: The rating value (1-5)
      responses:
        '204':
          description: Recipe rating updated successfully
        '400':
          description: Invalid rating value supplied
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Recipe not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security:
        - cookieAuth: []
  "/recipes/{id}/image":
    put:
      tags:
      - Recipes
      summary: Update recipe image by ID
      parameters: 
        - name: id
          required: true
          in: path
          description: Unique identifier of the recipe to query (UUID)
          schema:
            type: string
            format: uuid
      operationId: updateRecipeImage
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                image:
                  type: string
                  format: binary
                  description: The image file to upload.
      responses:
        '204':
          description: Recipe image updated successfully
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '403':
          description: Not authorized to update this recipe image
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Recipe not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported media type (e.g., not an image file)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security:
        - cookieAuth: []
    get:
      tags:
      - Recipes
      summary: Get recipe image by ID
      parameters: 
        - name: id
          required: true
          in: path
          description: Unique identifier of the recipe to query (UUID)
          schema:
            type: string
            format: uuid
        - name: size
          in: query
          description: Desired image size (e.g., "thumbnail", "medium", "large")
          required: false
          schema:
            "$ref": "#/components/schemas/ImageSize"
      operationId: getRecipeImage
      responses:
        '200':
          description: Recipe image retrieved successfully
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
        '404':
          description: Recipe or recipe image not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/recipes/{id}/comments":
    post:
      tags:
      - Recipes
      summary: Add comment to recipe by ID
      parameters: 
        - name: id
          required: true
          in: path
          description: Unique identifier of the recipe to query (UUID)
          schema:
            type: string
            format: uuid
      operationId: addRecipeComment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - content
              properties:
                content:
                  type: string
                  description: The content of the comment to add.
      responses:
        '201':
          description: Comment added successfully
          headers:
            Location:
              description: URL of the newly created resource
              schema:
                type: string
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Comment"
        '400':
          description: Invalid comment payload supplied
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Recipe not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security:
        - cookieAuth: []
    get:
      tags:
      - Recipes
      summary: List comments for recipe by ID
      parameters: 
        - name: id
          required: true
          in: path
          description: Unique identifier of the recipe to query (UUID)
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          description: The page number to retrieve (starting from 0)
          required: false
          schema:
            type: integer
            minimum: 0
        - name: pageSize
          in: query
          description: The number of items per page (default is 25)
          required: false
          schema:
            type: integer
            minimum: 25
            maximum: 100
      operationId: listRecipeComments
      responses:
        200:
          description: Comments retrieved successfully
          content:
            application/json:
              schema:
                type: array
                items:
                  "$ref": "#/components/schemas/Comment"
          headers:
            X-Pagination:
              description: Pagination metadata in JSON format
              schema:
                "$ref": "#/components/schemas/PaginationMetadata"
        404:
          description: Recipe not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/recipes/{recipeId}/comments/{commentId}":
    delete:
      tags:
      - Recipes
      summary: Delete recipe comment by ID
      parameters: 
        - name: recipeId
          required: true
          in: path
          description: Unique identifier of the recipe to query (UUID)
          schema:
            type: string
            format: uuid
        - name: commentId
          required: true
          in: path
          description: Unique identifier of the comment to delete (UUID)
          schema:
            type: string
            format: uuid
      operationId: deleteRecipeComment
      responses:
        '204':
          description: Recipe comment deleted successfully
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '403':
          description: Not authorized to delete this comment
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Recipe or comment not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security:
        - cookieAuth: []
  "/recipes/showcase":
    get:
      tags:
      - Recipes
      summary: List featured recipes
      operationId: listShowcaseRecipes
      responses:
        '200':
          description: Featured recipes retrieved successfully
          content:
            application/json:
              schema:
                type: array
                items:
                  "$ref": "#/components/schemas/Recipe"
  "/recipes/featured":
    get:
      tags:
      - Recipes
      summary: Get featured recipe
      operationId: getFeaturedRecipe
      responses:
        '200':
          description: Featured recipe retrieved successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Recipe"
    put:
      tags:
      - Recipes
      summary: Update featured recipe
      operationId: updateFeaturedRecipe
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - recipeId
              properties:
                recipeId:
                  type: string
                  format: uuid
                  description: The unique identifier of the recipe to set as featured.
      responses:
        '204':
          description: Featured recipe updated successfully
        '401':
          description: No active session
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '403':
          description: Not authorized to update the featured recipe
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Recipe not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '400':
          description: Format error (e.g., invalid UUID)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
      security:
        - cookieAuth: []

components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: .AspNetCore.Identity.Application
  schemas:
    HealthReport:
      type: object
      properties:
        status:
          type: string
          example: ok
        services:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                example: database
              healthy:
                type: boolean
                example: true
              message:
                type: string
                nullable: true
    CreateRecipeRequest:
      title: CreateRecipeRequest
      type: object
      required:
      - title
      properties:
        title:
          type: string
          description: The title of the new recipe
          minLength: 1
        description:
          type: string
          description: Short textual summary of the recipe
        allergens:
          $ref: '#/components/schemas/AllergenList'
        steps:
          type: array
          description: List of steps to prepare the recipe
          items:
            type: string
        ingredients:
          type: array
          description: List of ingredients needed for the recipe
          items:
            $ref: '#/components/schemas/Ingredient'
    ImageSize:
      type: string
      enum:
        - thumbnail
        - medium
        - large
    PaginationMetadata:
      type: object
      properties:
        totalCount:
          type: integer
          description: Total number of items available
        pageNumber:
          type: integer
          description: Current page number (starting from 0)
        pageCount:
          type: integer
          description: Total number of pages available
        pageSize:
          type: integer
          description: Number of items per page
        hasNextPage:
          type: boolean
          description: Indicates if there is a next page available
        hasPreviousPage:
          type: boolean
          description: Indicates if there is a previous page available
    UserRole:
      type: string
      enum:
        - Admin
      description: 'Role to assign to the user (currently supported: "Admin")'

    Recipe:
      type: object
      required:
      - id
      - title
      - createdAt
      - averageRating
      - creatorDisplayName
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier of the recipe
        creatorDisplayName:
          type: string
          description: The display name of the recipe's author
        title:
          type: string
          description: Title of the recipe
        description:
          type: string
          description: Short description of the recipe
          nullable: true
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the recipe was created
        updatedAt:
          type: string
          format: date-time
          description: Timestamp when the recipe was last updated
          nullable: true
        allergens:
          $ref: '#/components/schemas/AllergenList'
        steps:
          type: array
          description: List of steps to prepare the recipe
          items:
            type: string
        ingredients:
          type: array
          description: List of ingredients needed for the recipe
          items:
            $ref: '#/components/schemas/Ingredient'
        averageRating:
          type: number
          format: double
          description: The average rating of the recipe (1-5)
          nullable: false
    RecipeSummary:
      type: object
      required:
      - id
      - title
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier of the recipe
        title:
          type: string
          description: Title of the recipe


    AllergenList:
      type: array
      description: List of allergens present in the recipe
      items:
        $ref: '#/components/schemas/Allergen'
      nullable: false
      uniqueItems: true
      minItems: 0
      maxItems: 14
    AllergenFilter:
      type: array
      description: List of allergens to search for in the recipe
      items:
        $ref: '#/components/schemas/Allergen'
      nullable: true
      uniqueItems: true
      minItems: 0
      maxItems: 14
    Allergen:
      type: string
      enum:
        - GLUTEN
        - CRUSTACEANS
        - EGGS
        - FISH
        - PEANUTS
        - SOYBEANS
        - MILK
        - NUTS
        - CELERY
        - MUSTARD
        - SESAMESEEDS
        - SULPHURDIOXIDE
        - LUPIN
        - MOLLUSCS
    
    Ingredient:
      type: object
      required:
        - name
        - quantity
        - unitOfMeasurement
      properties:
        name:
          type: string
          description: The name of the ingredient
          nullable: false
        quantity:
          type: number
          description: The quantity of the ingredient needed for the recipe
          nullable: false
        unitOfMeasurement:
          type: string
          description: The unit of measure for the ingredient quantity (e.g., grams, cups, tablespoons)
          nullable: false
    Comment:
      type: object
      required:
        - id
        - content
        - createdAt
        - author
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier of the comment
        content:
          type: string
          description: Text content of the comment
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the comment was created
        author:
          type: string
          description: Display name of the comment author
    RegisterRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
          description: The user's email address (used as username)
        password:
          type: string
          format: password
          minLength: 6
          description: Password (min 6 characters, at least one digit)
        displayName:
          type: string
          description: The user's display name
    LoginRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
          description: The user's email address (used as username)
        password:
          type: string
          format: password
          description: The user's password
    IdentityError:
      type: object
      properties:
        code:
          type: string
          description: Machine-readable error code (e.g. DuplicateEmail)
        description:
          type: string
          description: Human-readable error description 
    LoginResponse:
      type: object
      required:
        - id
        - email
        - displayName
      properties:
        id:
          type: string
          format: uuid
          description: Identifier of the logged-in user
        email:
          type: string
          format: email
          description: Email address of the logged-in user
        displayName:
          type: string
          description: Display name of the logged-in user
    Profile:
      type: object
      required:
        - id
        - email
        - displayName
        - registeredAt
      properties:
        id:
          type: string
          format: uuid
          description: Identifier of the logged-in user
        email:
          type: string
          format: email
          description: Email address of the user
        displayName:
          type: string
          description: Display name of the logged-in user
        registeredAt:
          type: string
          format: date-time
          description: Registration timestamp of the logged-in user
    UpdateProfileRequest:
      type: object
      properties:
        displayName:
          type: string
          nullable: true
          description: The user's display name
        password:
          type: string
          format: password
          minLength: 6
          nullable: true
          description: New password for the user account
        profileImage:
          type: string
          format: binary
          nullable: true
          description: The profile image file to upload.
        email:
          type: string
          format: email
          nullable: true
          description: The user's email address (used as username)
    ErrorResponse:
      type: object
      properties:
        message:
          type: string
          description: A human-readable error message describing what went wrong.


