info:
  name: recipes
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:9393/api/recipes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
